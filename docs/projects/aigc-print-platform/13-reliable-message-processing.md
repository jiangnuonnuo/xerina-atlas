---
title: 我如何处理上游失败、重试和死信
type: project-chapter
project: aigc-print-platform
group: 任务可靠性
order: 13
description: 从失败分类、消息确认到重试上限和死信处置，说明任务为什么能够从异常中恢复或明确结束。
sidebar: true
layout: project-doc
---

## 我先回答一个问题：失败之后怎么办

AI 任务的失败不是一种情况。网络短暂抖动、上游 5xx、账号限流、参数错误和内容审核拒绝，恢复方式完全不同。上游支持基于稳定 ID 的防重，即使同一任务因消息重投再次提交，也不会创建第二个有效任务；但无边界重试仍会浪费消费者、连接和上游配额，所以本地仍然必须做失败分类和次数控制。

因此我把失败处理拆成三步：**分类、有限重试、最终处置**。

我不会把“重试”理解成把消息重新放回原队列。真正需要处理的是：这次失败有没有产生外部副作用、多久后再试、最多再试几次、重试期间是否会挡住其他任务，以及最后谁来接手无法自动判断的任务。

![AI 任务消息可靠性链路](./assets/reliable-message-processing.svg)

[查看消息从业务队列进入延迟重试和死信的 GIF](./assets/reliable-message-processing.gif)。动态版本强调的是消息路径，不代表每个任务都会经过所有分支。

## 一条消息的完整生命周期

### 0. 我先把消息路径分开

业务队列只承载第一次执行和重新执行的任务，延迟重试队列只负责等待，最终死信队列只负责收容和告警。不同重试轮次使用不同的延迟级别，例如短延迟、较长延迟和最终人工处理，避免一条长延迟消息挡住同一个队列里本来可以立即执行的消息。

每次投递都带上任务 ID、投递 ID、重试次数、首次创建时间、最近错误分类和追踪标识。重试不是创建一条新的业务任务，而是创建一次新的投递尝试；业务幂等键始终保持不变。

消息拓扑、延迟重试、最终死信和本地状态投影见上面的可靠性链路图。消息中保留任务 ID、投递 ID 和稳定的上游请求 ID，重复投递时沿用原 ID，不生成新的业务任务。

延迟队列可以通过 TTL + 死信转发或延迟消息能力实现，重点不是具体插件，而是让等待发生在队列层，不占用消费者线程。不同延迟级别最好拆成不同队列，避免长延迟消息挡住短延迟消息。

### 1. 生产者先落库，再发布消息

生产者投递的不是“立即调用上游”的指令，而是一个带有任务 ID、子任务 ID、业务类型、幂等键和输入引用的执行意图。生产者先用唯一索引写入主任务和子任务，消费者收到消息后以本地任务状态为准，不能把消息本身当成已经受理的业务事实。

本项目没有把数据库写入和 RabbitMQ 发布放在同一个数据库事务中。实际顺序是“生产者任务入库、发布 RabbitMQ 消息、等待 Publisher Confirm”。发布端还要检查 mandatory/return，确认消息不是不可路由；Confirm 失败或结果未知时，任务保留 `PUBLISH_PENDING`，沿用原 `taskId`、`subTaskId` 和上游请求 ID 重发，不创建新的业务任务。

我在排查“任务已创建但没有消费”的问题时，会先查任务状态、发布状态和 Publisher Confirm，再查队列堆积和消费者日志，而不是让用户重新提交。数据库写入成功但消息发布失败时，必须沿用原任务 ID 进入发布重试或人工修复；不能因为发布失败就重新创建一条业务任务。

Confirm 只说明 broker 已接收消息，唯一索引只阻止重复建档，幂等校验只保证同一个业务身份可以安全重试；三者都不能单独保证数据库和消息原子一致。本项目追求的是“任务事实先落库、消息至少一次投递、重复执行无有效副作用”。

### 2. 消费成功后再确认消息

消费者取到消息后先做本地幂等和状态检查，再执行上游调用。消息消费、任务 ID 检查、上游 ID 防重和本地事实落库见下面的决策流程图。

![消息消费与上游 ID 防重流程](./assets/aigc-consumer-decision-flow.svg)

如果消费者在确认前宕机，消息可以再次投递；因此“至少一次投递”必须和“重复消费无副作用”一起设计，单独依赖 ack 并不能解决重复问题。

消费者对异常的处理也不能只有一个 `requeue` 开关：

- 参数错误、无法反序列化和明确的业务拒绝，记录失败原因后拒绝重新入队，避免坏消息无限循环。
- 网络超时、上游 5xx 和明确的 429，写入重试信息后投递到延迟队列，原消息确认掉，避免主队列被同一条消息反复占住。
- 数据库暂时不可用时，不能在本地状态未保存的情况下确认消息；让消息重新投递，同时通过退避保护数据库。
- 上游已经受理但本地保存失败时，沿用原任务 ID 和上游请求 ID 重新提交或查询；上游按原 ID 识别重复请求并保持防重，本地再根据实际响应补写外部任务 ID，不创建新的业务任务。

这里的 `ack` 位置很关键：业务已经把失败归类并保存后，原消息才可以确认；如果只是因为异常被捕获就 ack，消息会消失；如果所有异常都 `requeue`，坏消息会在主队列里高速循环。即使消息重新投递，消费者也要复用原任务 ID 和上游请求 ID；本地任务 ID 防止重复状态推进，上游 ID 防重防止重复创建外部任务。

### 3. 只对可恢复异常重试

| 失败类型 | 例子 | 处理方式 |
| --- | --- | --- |
| 瞬时故障 | 网络抖动、连接超时、上游 5xx | 延迟后重试 |
| 上游限流 | 429、账号队列繁忙 | 延迟更长，并降低对应账号并发 |
| 参数问题 | 参数缺失、格式不合法、反序列化失败 | 直接失败，不重试 |
| 业务拒绝 | 审核拒绝、余额不足 | 直接失败，保留可读原因 |
| 明确不存在 | 上游返回任务不存在等确定性错误 | 直接失败或进入人工处理 |

失败分类应在消费者边界完成，而不是把所有异常都交给一个通用重试器。这样可以避免参数错误占满重试队列，也能让告警区分“上游故障”和“业务输入错误”。

我尤其关注“结果未知”这一类异常。比如 HTTP 读取超时发生在上游已经创建任务之后，本地没有拿到外部任务 ID。这时必须继续使用原任务 ID 或请求 ID，让上游按原 ID 识别重复请求并保持防重，再由本地根据实际响应补齐外部任务 ID；如果上游暂时无法查询，就让任务进入人工或定时对账路径，而不是生成新的业务 ID。

### 4. 延迟重试不能占住消费者线程

对于可以恢复的错误，消息进入独立的延迟重试队列，等待 30 秒或更长退避时间后再回到业务队列。等待期间不占用消费者线程，业务主队列也不会被失败消息卡住。

延迟时间不是固定 sleep，而是根据失败类型和重试次数计算。例如限流错误需要比普通网络抖动更长的退避，连续失败时还要叠加随机抖动，避免多个消费者在同一秒重新冲击上游。总重试时长也必须受任务最大执行时长约束，不能因为不断重试让任务永远处于处理中。

推荐的重试边界是：

- 第一次失败：短延迟重试，处理偶发网络抖动。
- 第二次失败：延长退避，给上游恢复时间。
- 超过最大轮次：进入最终死信，不再自动调用上游。

重试次数、重试间隔和总超时必须是配置项，并且要能在消息属性或本地任务记录中查询。不能依赖无限 `requeue=true`，否则一条坏消息可能持续占用队列。

每次重试前都会重新检查任务状态。如果用户已经取消、其他消费者已经成功，或者任务超过了最大执行时间，即使延迟消息再次到达，也只做状态记录和 ack，不再调用上游。

### 5. 死信是收容和人工入口

进入最终死信的消息只做四件事：记录失败原因、更新任务终态、触发告警、等待人工重放或忽略。死信消费者不应该再次无条件调用上游，否则失败消息会在死信和上游之间循环。

人工重放前需要确认：参数是否修复、上游是否恢复、是否允许重新扣费，以及原任务当前状态是否仍允许执行。重放要生成新的投递记录，但沿用原业务任务的幂等键。

死信处理还需要防止“人工重放变成第二个重试器”。我会要求重放带操作人、操作原因和新的投递 ID，并重新经过状态检查和限流；一次重放失败后不能自动绕过最大重试次数继续循环。对批量任务，人工重放优先选择单个失败子任务，而不是整批重新执行。

## 我如何定位一次失败

遇到任务失败时，我会按时间顺序串起四类记录：

1. 任务记录：当前状态、重试次数、外部任务 ID和最后一次错误分类。
2. 消息记录：发布是否确认、进入哪个队列、被消费几次、最后一次投递 ID。
3. 上游记录：请求开始和结束时间、HTTP 状态、超时阶段、是否拿到外部任务 ID。
4. 结果记录：回调是否到达、结果是否转存、文件是否可以访问。

如果只记录一条异常堆栈，很难判断应该重试提交、重试查询、重试下载还是直接人工处理；把失败阶段记录下来，恢复动作才不会误伤已经成功的部分。

## 这项工作的价值

我把“调用失败”变成了有规则的处理路径：短暂故障自动恢复，不可恢复故障快速失败，无法自动判断的故障进入可追踪的人工入口。任务最终成功率需要定义为统计窗口内进入成功终态的任务数除以成功和失败终态任务数之和，不能把重试次数或消息消费次数混入分母。

这个结果不是单纯增加重试次数得到的。无限重试只能延迟失败，还会放大上游压力；有效的提升来自三件事：把可以恢复的异常送到正确的延迟路径，把结果未知的调用和已明确失败的调用分开，把超过边界的消息送入死信后停止自动副作用。这样成功率提升的同时，失败任务也更容易解释和恢复。

---

*下一章：[状态机、回调和幂等如何配合](./14-state-machine-and-idempotency)*
