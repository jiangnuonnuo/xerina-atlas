---
title: 30 · 平台字段目录结构治理
type: project-chapter
project: baozun-field-platform
order: 30
group: 目录治理
description: 目录树的结构不变量、写入查询分离与并发一致性。
layout: project-doc
---

## 平台字段目录结构治理

> 目录树不是"存下来"就完了，它要可查询、可演进、可一致。这一节讲结构不变量、生命周期与并发一致性。

---

## 结构不变量

字段目录是一棵树，但我们要维护的是**不变量**而不是整棵树：

- **唯一父级**：每个节点有且仅有一个直接父级（根除外）；
- **祖先可推导**：祖先链可由父级关系逐步推导；
- **兄弟有序**：同父下的子节点有稳定顺序；
- **类型可识别**：节点是"分组"还是"字段"有明确类型。

不变量让"树对不对"可被校验，而不是靠人眼。

## 写入与查询分离

写入和查询走不同路径：

- **写入**：维护直接父级关系，校验不变量；
- **查询**：按需展开祖先链 / 子树，不要求全树常驻内存。

这样写入不会因树变大而变慢，查询也不会因部分写入而读到半成品。

<InteractiveDiagram
  title="字段目录树的写入、查询与一致性治理"
  src="/media/projects/baozun-field-platform/diagrams/tree-governance/index.html?embed=1"
  poster="/media/projects/baozun-field-platform/diagrams/tree-governance/preview.png"
  description="直接父级关系与祖先关系的双维护。"
/>

## 生命周期与并发一致性

节点有生命周期：草稿 → 已发布 → 已归档。并发写入时：

- 用**版本号 / CAS** 兜住同一节点的并发修改；
- 祖先链变更走"先校验后提交"，避免悬空引用；
- 大批量写入用**分段锁 / 行锁**，缩小冲突面。

> 一句话收尾：目录治理的本质，是把"树"当成"带不变量的数据结构"来维护，而不是当成文档来存。
